package org.xiaoyu.core.config;

/**
 * file config interface
 */
public interface RpcConstant {
    // 配置文件前缀
    String CONFIG_FIFE_PREFIX = "rpc";

    // 默认的配置版本
    String DEFAULT_CONFIG_VERSION = "1.0.0";
}
